/* eslint-disable react/prop-types */
import { useEffect, useMemo, useState } from 'react';
import { BusFront, CarFront, ChevronLeft, ChevronRight, Clock3, Filter, MapPinned, MoreVertical, Plus, Search, UsersRound } from 'lucide-react';

import { Badge } from '@components/components/ui/badge';
import { Button } from '@components/components/ui/button';
import { Card, CardContent } from '@components/components/ui/card';
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from '@components/components/ui/dropdown-menu';
import { Input } from '@components/components/ui/input';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@components/components/ui/select';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from './EventTable';
import EventGuestsNav from './EventGuestsNav';

const idOf = (value) => String(value?._id || value || '');
const routeKey = (item) => `${item.origin || ''}|||${item.destination || ''}`;
const statuses = ['Pending Assignment', 'Assigned', 'Scheduled', 'Confirmed', 'Completed', 'Cancelled'];
const statusTone = (status) => ['Assigned', 'Confirmed', 'Completed'].includes(status) ? 'border-emerald-200 bg-emerald-50 text-emerald-700' : status === 'Scheduled' ? 'border-blue-200 bg-blue-50 text-blue-700' : status === 'Cancelled' ? 'border-red-200 bg-red-50 text-red-700' : 'border-amber-200 bg-amber-50 text-amber-700';
const scheduleLabel = (value) => { if (!value) return { date: '-', time: '' }; const date = new Date(value); if (Number.isNaN(date.getTime())) return { date: '-', time: '' }; return { date: new Intl.DateTimeFormat('en-GB', { day: '2-digit', month: 'short', year: 'numeric' }).format(date), time: new Intl.DateTimeFormat('en-US', { hour: '2-digit', minute: '2-digit' }).format(date) }; };

export default function EventTransportPanel({ assignments, guests, vehicles, onGuestTab, onAddTransport, onEditTransport, onAddVehicle, onEditVehicle }) {
  const [search, setSearch] = useState('');
  const [routeFilter, setRouteFilter] = useState('all');
  const [statusFilter, setStatusFilter] = useState('all');
  const [pendingOnly, setPendingOnly] = useState(false);
  const [page, setPage] = useState(1);
  const pageSize = 10;
  const guestMap = useMemo(() => new Map(guests.map((item) => [idOf(item), item])), [guests]);
  const vehicleMap = useMemo(() => new Map(vehicles.map((item) => [idOf(item), item])), [vehicles]);
  const routes = useMemo(() => Array.from(new Map(assignments.filter((item) => item.origin || item.destination).map((item) => [routeKey(item), { key: routeKey(item), label: [item.origin, item.destination].filter(Boolean).join(' → ') }])).values()), [assignments]);
  const filtered = useMemo(() => assignments.filter((item) => {
    const guest = guestMap.get(idOf(item.guest));
    const vehicle = vehicleMap.get(idOf(item.vehicle));
    const term = search.trim().toLowerCase();
    if (term && !`${guest?.name || ''} ${item.origin || ''} ${item.destination || ''} ${vehicle?.name || ''} ${vehicle?.registrationNumber || ''}`.toLowerCase().includes(term)) return false;
    if (routeFilter !== 'all' && routeKey(item) !== routeFilter) return false;
    if (statusFilter !== 'all' && item.status !== statusFilter) return false;
    if (pendingOnly && item.status !== 'Pending Assignment') return false;
    return true;
  }), [assignments, guestMap, pendingOnly, routeFilter, search, statusFilter, vehicleMap]);
  const pages = Math.max(1, Math.ceil(filtered.length / pageSize));
  const pageRows = filtered.slice((page - 1) * pageSize, page * pageSize);
  useEffect(() => setPage(1), [pendingOnly, routeFilter, search, statusFilter]);
  useEffect(() => { if (page > pages) setPage(pages); }, [page, pages]);
  const required = guests.filter((item) => item.transportRequired || (item.hospitalityNeeds || []).some((need) => /pickup|drop|transport|transfer/i.test(need))).reduce((total, item) => total + Number(item.memberCount || 1), 0);
  const assigned = assignments.filter((item) => !['Pending Assignment', 'Cancelled'].includes(item.status)).reduce((total, item) => total + Number(item.guestCount || 1), 0);
  const summaries = [{ label: 'Transport Required', value: required, icon: BusFront, tone: 'text-emerald-600 bg-emerald-50' }, { label: 'Assigned', value: assigned, icon: CarFront, tone: 'text-blue-600 bg-blue-50' }, { label: 'Pending Assignment', value: Math.max(0, required - assigned), icon: Clock3, tone: 'text-amber-600 bg-amber-50' }, { label: 'Vehicles', value: vehicles.length, icon: BusFront, tone: 'text-violet-600 bg-violet-50' }, { label: 'Routes', value: routes.length, icon: MapPinned, tone: 'text-cyan-600 bg-cyan-50' }];
  const actions = <div className="flex max-w-full shrink-0 flex-nowrap gap-2 overflow-x-auto pb-0.5"><div className="relative w-44 shrink-0"><Search className="absolute left-3 top-2.5 h-4 w-4 text-muted-foreground" /><Input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search guest / family" className="h-9 pl-9 text-xs" /></div><Select value={routeFilter} onValueChange={setRouteFilter}><SelectTrigger className="h-9 w-32 shrink-0 text-xs"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">All Routes</SelectItem>{routes.map((route) => <SelectItem key={route.key} value={route.key}>{route.label}</SelectItem>)}</SelectContent></Select><Select value={statusFilter} onValueChange={setStatusFilter}><SelectTrigger className="h-9 w-36 shrink-0 text-xs"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">Transport Status</SelectItem>{statuses.map((status) => <SelectItem key={status} value={status}>{status}</SelectItem>)}</SelectContent></Select><Button variant={pendingOnly ? 'default' : 'outline'} size="sm" className="h-9 shrink-0 gap-2" onClick={() => setPendingOnly((value) => !value)}><Filter className="h-4 w-4" />More Filters</Button></div>;

  return <Card id="transport-plan" className="crm-card overflow-hidden"><EventGuestsNav active="transport" onSelect={onGuestTab} actions={actions} /><CardContent className="p-4"><div className="mb-4 flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div className="grid flex-1 grid-cols-2 gap-2 rounded-lg border border-border p-3 sm:grid-cols-5">{summaries.map(({ label, value, icon: Icon, tone }, index) => <div key={label} className={`flex items-center gap-2 px-2 ${index ? 'sm:border-l sm:border-border' : ''}`}><span className={`grid h-9 w-9 shrink-0 place-items-center rounded-full ${tone}`}><Icon className="h-4 w-4" /></span><div><p className="text-[10px] font-semibold text-muted-foreground">{label}</p><p className="text-xl font-bold text-foreground">{value.toLocaleString('en-IN')}</p></div></div>)}</div><div className="flex shrink-0 gap-2"><Button type="button" variant="outline" className="gap-2" onClick={onAddVehicle}><CarFront className="h-4 w-4" />Add Vehicle</Button>{vehicles.length ? <DropdownMenu><DropdownMenuTrigger asChild><Button type="button" variant="outline" size="icon" aria-label="Edit saved vehicles"><MoreVertical className="h-4 w-4" /></Button></DropdownMenuTrigger><DropdownMenuContent align="end">{vehicles.map((vehicle) => <DropdownMenuItem key={idOf(vehicle)} onSelect={() => onEditVehicle(vehicle)}>Edit {vehicle.name}</DropdownMenuItem>)}</DropdownMenuContent></DropdownMenu> : null}<Button type="button" className="gap-2 bg-blue-600 hover:bg-blue-700" onClick={onAddTransport}><Plus className="h-4 w-4" />Add Transport</Button></div></div>
    <div className="overflow-hidden rounded-lg border border-border"><div className="border-b border-border px-4 py-3"><h2 className="text-sm font-bold text-foreground">Transport Plan</h2></div><div className="overflow-x-auto"><Table className="min-w-[1100px] table-fixed text-xs"><colgroup><col className="w-[16%]" /><col className="w-[20%]" /><col className="w-[11%]" /><col className="w-[12%]" /><col className="w-[14%]" /><col className="w-[9%]" /><col className="w-[9%]" /><col className="w-[9%]" /></colgroup><TableHeader><TableRow className="bg-muted/30 hover:bg-muted/30"><TableHead className="pl-6">Guest / Family</TableHead><TableHead>Route</TableHead><TableHead>Pickup / Drop</TableHead><TableHead>Vehicle</TableHead><TableHead>Schedule</TableHead><TableHead className="text-center"><span className="relative -left-4">Guests</span></TableHead><TableHead>Status</TableHead><TableHead className="pr-6 text-right">Action</TableHead></TableRow></TableHeader><TableBody>{pageRows.length ? pageRows.map((item, index) => { const guest = guestMap.get(idOf(item.guest)); const vehicle = vehicleMap.get(idOf(item.vehicle)); const schedule = scheduleLabel(item.scheduledAt); return <TableRow key={item._id || `pending-${idOf(item.guest)}`}><TableCell className="pl-6"><div className="flex items-center gap-2"><span className={`grid h-8 w-8 place-items-center rounded-full ${index % 2 ? 'bg-amber-50 text-amber-600' : 'bg-emerald-50 text-emerald-600'}`}><UsersRound className="h-4 w-4" /></span><span className="font-semibold text-foreground">{guest?.name || 'Guest record'}</span></div></TableCell><TableCell>{item.origin || item.destination ? <div className="flex items-center gap-2"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-cyan-50 text-cyan-600"><MapPinned className="h-4 w-4" /></span><p className="max-w-48 font-medium text-foreground">{item.origin || '-'} <span className="text-muted-foreground">→</span> {item.destination || '-'}</p></div> : <span className="text-muted-foreground">Route pending</span>}</TableCell><TableCell className="font-medium text-blue-700">{item.transferType || '-'}</TableCell><TableCell>{vehicle ? <div className="flex items-center gap-2"><CarFront className="h-4 w-4 text-blue-600" /><div><p className="font-semibold text-foreground">{vehicle.name}</p><p className="text-[10px] text-blue-700">{vehicle.registrationNumber || vehicle.vehicleType || '-'}</p></div></div> : <span className="text-muted-foreground">Not Assigned</span>}</TableCell><TableCell><p className="flex items-center gap-2 font-medium"><Clock3 className="h-3.5 w-3.5 text-muted-foreground" />{schedule.date}</p>{schedule.time ? <p className="mt-0.5 pl-5 text-[10px] text-foreground">{schedule.time}</p> : null}</TableCell><TableCell className="text-center font-semibold"><span className="relative -left-4 inline-block">{item.guestCount || guest?.memberCount || 1}</span></TableCell><TableCell><Badge variant="outline" className={`rounded px-2 py-0.5 text-[9px] ${statusTone(item.status)}`}>{item.status || 'Pending Assignment'}</Badge></TableCell><TableCell className="pr-6 text-right"><div className="flex justify-end"><Button variant="ghost" size="sm" className="h-8 gap-1 px-2 text-blue-600" onClick={() => onEditTransport(item)}>View<ChevronRight className="h-3.5 w-3.5" /></Button>{item._id ? <DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" size="icon" className="h-8 w-8"><MoreVertical className="h-4 w-4" /></Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onSelect={() => onEditTransport(item)}>Edit transport</DropdownMenuItem></DropdownMenuContent></DropdownMenu> : null}</div></TableCell></TableRow>; }) : <TableRow><TableCell colSpan={8} className="h-40 text-center"><BusFront className="mx-auto mb-2 h-8 w-8 text-muted-foreground" /><p className="font-semibold text-foreground">No transport records found</p><p className="mt-1 text-[11px] text-muted-foreground">Add transport needs to guests or create an assignment.</p></TableCell></TableRow>}</TableBody></Table></div><div className="flex flex-col gap-3 border-t border-border px-4 py-3 text-xs sm:flex-row sm:items-center sm:justify-between"><span className="text-muted-foreground">Showing {filtered.length ? (page - 1) * pageSize + 1 : 0} to {Math.min(page * pageSize, filtered.length)} of {filtered.length} transport records</span><div className="flex items-center gap-2"><Button variant="outline" size="icon" className="h-8 w-8" disabled={page === 1} onClick={() => setPage((value) => value - 1)}><ChevronLeft className="h-4 w-4" /></Button><span className="grid h-8 min-w-8 place-items-center rounded-md bg-emerald-50 px-2 font-semibold text-emerald-700">{page}</span><Button variant="outline" size="icon" className="h-8 w-8" disabled={page === pages} onClick={() => setPage((value) => value + 1)}><ChevronRight className="h-4 w-4" /></Button></div></div></div><div className="mt-4 flex items-center gap-2 rounded-lg border border-blue-100 bg-blue-50/50 px-4 py-3 text-xs text-blue-700"><span className="grid h-5 w-5 shrink-0 place-items-center rounded-full bg-blue-600 text-[11px] font-bold text-white">i</span>Guests marked Transport Required in the Guest List are managed here for pickup, drop and route coordination.</div></CardContent></Card>;
}
